public class PerfectNumber {
     public static boolean isPerfectNumber(int number) {
         if (number >= 1) {
             int sum = 0;
             for (int i = 1; i < number; i++) {
                 if (number % i == 0) {
                     sum = sum + i;
                 }
             }
             if (number == sum)
                 return true;
             
         }return false;
     }
 }
